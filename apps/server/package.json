{
  "name": "@neatly/api",
  "version": "0.0.0",
  "private": true,
  "description": "Neatly HTTP API.",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env src/server.ts",
    "build": "tsc",
    "start": "node --env-file-if-exists=.env dist/server.js",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=24.19.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "catalog:"
  }
}
